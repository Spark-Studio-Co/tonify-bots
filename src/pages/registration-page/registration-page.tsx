import { Layout } from "@/shared/layouts/layout";
import RegistrationForm from "@/widgets/forms/registration-form/registration-form";

export const RegistrationPage = () => {
  return (
    <Layout>
      <RegistrationForm />
    </Layout>
  );
};
